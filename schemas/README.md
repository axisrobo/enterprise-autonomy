# Schemas

This directory reserves the public layout for schema overviews. Authoritative schema definitions live in the private `enterprise-autonomy-ee` repository and in each product repository.

Public example payloads (for example runnable demo requests) live in `examples/` and `adapters/`. Do not place internal schema artifacts in this directory.
