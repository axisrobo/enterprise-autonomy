# Architecture

This directory reserves the public layout for architecture overviews. Authoritative architecture documents and internal design live in the private `enterprise-autonomy-ee` repository.

Public material may describe product composition and roles at an adoption level (see `reference-stacks/` and `vertical-slices/`). Do not place internal architecture or deployment topology in this directory.
